
public class Todo {
//* create a pool of candidates
//* some algorithm to walk through pool and find best match (greedy first)
//* Make sure AAA then BBB in same column shows up as ===--- not ======
	
	// Work on pool to generate all candidates then select best
	// Does "bestIn" belong on scorer?
	
	
}
